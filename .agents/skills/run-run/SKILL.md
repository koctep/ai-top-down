---
name: run-run
description: Start the repository application through its Makefile. Use when running the project locally.
---

# Run the Project

Run `make run` from the repository root.

- Prefer this target over toolchain-specific run commands.
- State any required configuration or environment variables before starting the process.
